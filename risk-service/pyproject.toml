[project]
name = "risk-service"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic",
    "pydantic-settings",
    "beanie",
    "motor",
    "scikit-learn", # For the ML model
    "joblib",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "httpx",
]